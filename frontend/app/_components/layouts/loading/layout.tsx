const Loading = () => {
  return(
    <div className='flex flex-col items-center justify-center w-full min-h-screen bg-transparent' data-theme='fantasy'>
      <span className='loading loading-spinner loading-lg'></span>
    </div>
  );
}

export default Loading;